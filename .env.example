# Optional: Provide your Gemini API key to enable live LLM extraction (gemini-3.8-flash)
# If not provided, the pipeline seamlessly operates in offline mode with high-precision local rule-based NER.
GEMINI_API_KEY=
